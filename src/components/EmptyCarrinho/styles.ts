import { styled } from '../../styles/stitches.config'

export const DivEmptyCarrinho = styled('div', {
    width: '25rem',
    height: '4rem',

    backgroundColor: '$--base---card',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
})